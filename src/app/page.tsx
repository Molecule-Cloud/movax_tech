/**
 * Home page
 * ----------------------------------------------------------------------------
 * TEMPORARY: this is a placeholder so the layout (navbar + marquee +
 * theming) can be reviewed and run on its own before the real sections —
 * Hero, Services, Process, Vision, Contact — are built in the next pass.
 * Nothing here is final.
 */
export default function Home() {
  return (
    <div className="flex min-h-[60vh] items-center justify-center px-6">
      <p className="font-mono text-sm uppercase tracking-wider text-foreground/50">
        Hero section — coming next
      </p>
    </div>
  );
}
 